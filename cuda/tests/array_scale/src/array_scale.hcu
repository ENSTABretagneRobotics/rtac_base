#pragma once

#include <rtac_base/types/ArrayScale.h>
using namespace rtac::types;

template <typename T, class ScaleT>
__global__ void get_scale(T* scaleData, ScaleT scale)
{
    scaleData[threadIdx.x] = scale(threadIdx.x)[0];
}




